{
  "name": "jacobworsoe-jekyll",
  "version": "1.0.0",
  "description": "Assets build for Jekyll site",
  "devDependencies": {
    "grunt": "^1.4.0",
    "grunt-contrib-concat": "^1.0.1",
    "grunt-contrib-sass": "^2.0.0",
    "grunt-contrib-uglify": "^4.0.1",
    "load-grunt-tasks": "^3.2.0"
  },
  "scripts": {
    "build": "grunt build",
    "build:css": "grunt sass",
    "build:js": "grunt concat uglify"
  }
}
